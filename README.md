# Document Intelligence Pipeline

A pipeline that accepts a PDF and produces a structured representation suitable for downstream
AI systems, with a web UI that makes the pipeline's routing and provenance inspectable.

Build in progress. This README is replaced by the full 1–2 page architecture write-up at delivery.
